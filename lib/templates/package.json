{
  "name": "{{name}}",
  "version": "0.0.1",
  "description": "A freshly threshed, viable seed.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "seeds-cli": "{{version}}"
  }
}
